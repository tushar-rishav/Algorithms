#include <stdio.h>


int main(){

	int r,c,di,j,k,l;
	char ch,numarr;
	int *row;
	scanf("%d %d",&r,&c); // row,column
	for(i=0;i<r;i++)
		row[i]=(int*)malloc(c*sizeof(int));
		
	for
	
	printf("\n D= %c",ch );

return 0;
}
