#include <stdio.h>
#include <stdlib.h>
void filter(int *num,int size){

	int temp=num[0],i;
	for(i=1;i<size;i++){
	
		if(temp==num[i])
			num[i]=-1;
		else
			temp=num[i];	
	
	}

}

int comp (const void * elem1, const void * elem2)
{
  int f = *((int*)elem1);
  int s = *((int*)elem2);
  if (f > s) return  1;
  if (f < s) return -1;
  
  return 0;
}
int main(){

int *num,t,c,i=0,j;
 scanf("%d",&t);
 c=t;
 num=(int*)malloc(t*sizeof(int));
 while(t--){
	 scanf("%d",&num[i]);
	 i++;
}
 qsort (num,c,sizeof(int), comp);
 for ( int i = 0; i < c; i++)
 	printf(" %d ",num[i]);
 	printf("\n");
 filter(num,c);

for(j=0;j<c;j++)
	if(num[j]>=0)	
		printf("%d\n",num[j]);

return 0;
}
