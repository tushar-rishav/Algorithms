#include<iostream>
#include<cstdlib>
using namespace std;
int cmpfunc (const void * a, const void * b)
{
   return ( *(int*)a - *(int*)b );
}
 
int Fact (int x) 
{ 
if (x == 0) 
	{return 1;}
else if(x==1)
	{return 1;} 
return (x * Fact(x-1)); 
}
int main()
{
	int T;
	cin>>T;
	while(T--)
	{
		int A[11];
		int K;
		for(int i=0;i<11;i++)
		{
			cin>>A[i];
		}
		cin>>K;
		qsort(A,11,sizeof(int),cmpfunc);
		int j=11-K;
		int count1=0;
		int count2=0;
		
		for(int i=0;i<11;i++)
		{
			if(A[i]==A[j])
			{
				count1++;
			}
		}
		
		for(int i=j;i<11;i++)
		{
			if(A[i]==A[j])
			{
				count2++;
			}
		}
		
		cout<<(Fact(count1)/(Fact(count1-count2)*Fact(count2)))<<endl;
	}
return 0;
} 
