#include <iostream>
#include <stdio.h>
#include <algorithm>
using namespace std;

int main() {
  	std::ios_base::sync_with_stdio(false);
	long t;
	long long c=0;
	string ch;
	scanf("%d",&t);
	while(t--){
	scanf("%s",&ch);
	c=count(ch.begin(),ch.end(),'4');
	printf("%lld\n",c);
	}	


	return 0;
}
