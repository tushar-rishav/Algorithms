#include <bits/stdc++.h>
using namespace std;

int main(){
//stringstream str;


	string str;
    string buf; // Have a buffer string
    getline (cin,str);
    stringstream ss(str); // Insert the string into a stream

    deque<string> tokens; // Create vector to hold our words

    while (ss >> buf)
        tokens.push_back(buf);
    while(!tokens.empty()){
    cout<<tokens.front()<<"\n";
    tokens.pop_front();
    }   
    tokens.clear(); 
	    getline (cin,str);
    ss(str); // Insert the string into a stream

    //deque<string> tokens; // Create vector to hold our words

    while (ss >> buf)
        tokens.push_back(buf);
    while(!tokens.empty()){
    cout<<tokens.front()<<"\n";
    tokens.pop_front();
    }         

/*
for (unsigned long i = 0; i < 10; i++){
str << i;
}*/

/*
std::ofstream outFile;
outFile.open("output.txt");

outFile << "Hello world"+str.str();
*/

return 0;
}
