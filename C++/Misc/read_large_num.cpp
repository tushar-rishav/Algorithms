#include <bits/stdc++.h>
#define NDEBUG
#define DEBUG(x) cout << '>' << #x << ':' << x << endl;
using namespace std;

int main()
{  
	std::ios_base::sync_with_stdio(false);
	char buffer[256];
	fgets(buffer,256,stdin);
	cout << atoi(buffer);
	return 0;
}